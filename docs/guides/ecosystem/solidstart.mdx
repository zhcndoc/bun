---
title: 使用 SolidStart 和 Bun 构建应用
sidebarTitle: "SolidStart 与 Bun"
mode: center
---

使用 `create-solid` 初始化一个 SolidStart 应用。你可以指定 `--solidstart` 标志来创建一个 SolidStart 项目，使用 `--ts` 来支持 TypeScript。提示选择模板时，选择 `basic` 以获取一个最小化的起始应用。

```sh terminal icon="terminal"
bun create solid my-app --solidstart --ts
```

```txt
┌
 Create-Solid v0.6.11
│
◇  项目名称
│  my-app
│
◇  请选择要使用的模板？
│  basic
│
◇  项目创建完成 🎉
│
◇  开始使用，请运行：─╮
│                        │
│  cd my-app             │
│  bun install           │
│  bun dev               │
│                        │
├────────────────────────╯
```

---

按照 `create-solid` CLI 的提示，安装依赖。

```sh terminal icon="terminal"
cd my-app
bun install
```

然后使用 `bun dev` 运行开发服务器。

```sh terminal icon="terminal"
bun dev
```

```txt
$ vinxi dev
vinxi v0.5.8
vinxi 启动开发服务器

  ➜ 本地地址:    http://localhost:3000/
  ➜ 网络地址:    使用 --host 以公开访问
```

打开 [localhost:3000](http://localhost:3000)。你对 `src/routes/index.tsx` 的任何修改都会自动热重载。

---

请参考 [SolidStart 官网](https://docs.solidjs.com/solid-start) 获取完整的框架文档。