---
title: 使用 Nuxt 和 Bun 构建应用
sidebarTitle: Nuxt 搭配 Bun
mode: center
---

Bun 无需额外配置即可支持 [Nuxt](https://nuxt.com)。使用官方的 `nuxi` CLI 初始化 Nuxt 应用。

```sh terminal icon="terminal"
bunx nuxi init my-nuxt-app
```

```txt
✔ 您想使用哪个包管理器？
bun
◐ 正在安装依赖...
bun install v1.3.3 (16b4bf34)
 + @nuxt/devtools@0.8.2
 + nuxt@3.7.0
 785 个包已安装 [2.67秒]
✔ 安装完成。
✔ 类型已生成于 .nuxt
✨ Nuxt 项目已使用 v3 模板创建。下一步：
 › cd my-nuxt-app
 › 使用 bun run dev 启动开发服务器
```

---

要启动开发服务器，请在项目根目录中运行 `bun --bun run dev`。此命令会执行 `package.json` 中 `"dev"` 脚本所定义的 `nuxt dev` 命令。

<Note>
  `nuxt` CLI 默认使用 Node.js；传入 `--bun` 参数则强制开发服务器使用 Bun 运行时。
</Note>

```sh terminal icon="terminal"
cd my-nuxt-app
bun --bun run dev
```

```txt
nuxt dev
Nuxi 3.6.5
Nuxt 3.6.5 搭配 Nitro 2.5.2
  > 本地:    http://localhost:3000/
  > 网络:    http://192.168.0.21:3000/
  > 网络:    http://[fd8a:d31d:481c:4883:1c64:3d90:9f83:d8a2]:3000/

✔ Nuxt DevTools 已启用 v0.8.0（实验性）
ℹ Vite 客户端预热 547 毫秒
✔ Nitro 构建完成 244 毫秒
```

---

开发服务器启动后，打开 [http://localhost:3000](http://localhost:3000) 即可查看应用。该应用会渲染 Nuxt 内置的 `NuxtWelcome` 模板组件。

开始开发你的应用时，可将 `app.vue` 中的 `<NuxtWelcome />` 替换为你自己的 UI。

<Frame>
  ![示例 Nuxt 应用运行在本地](https://github.com/oven-sh/bun/assets/3084745/2c683ecc-3298-4bb0-b8c0-cf4cfaea1daa)
</Frame>

---

对于生产环境构建，默认预设与 Bun 兼容，但 [Bun 预设](https://nitro.build/deploy/runtimes/bun)可以生成经过更好优化的构建结果。

```ts nuxt.config.ts icon="/icons/typescript.svg"
export default defineNuxtConfig({
  nitro: {
    preset: "bun", // [!code ++]
  },
});
```

或者，也可以使用环境变量设置预设：

```sh terminal icon="terminal"
NITRO_PRESET=bun bun run build
```

<Note>
  某些软件包提供了 Bun 专用导出，而 Nitro 使用默认预设时无法正确打包这些导出。请使用 Bun 预设，以确保这些软件包能够在生产环境构建中正常工作。
</Note>

构建完成后，启动服务器：

```sh terminal icon="terminal"
bun run ./.output/server/index.mjs
```

---

完整文档请参阅 [Nuxt 官网](https://nuxt.com/docs)。