---
title: 使用 fetch 发送 HTTP 请求
sidebarTitle: Bun 中的 Fetch
mode: center
---

Bun 实现了 Web 标准的 [`fetch`](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API) API，用于发送 HTTP 请求。要向某个 URL 发送 `GET` 请求：

```ts fetch.ts icon="/icons/typescript.svg"
const response = await fetch("https://bun.com");
const html = await response.text(); // HTML 字符串
```

---

要向 API 端点发送 `POST` 请求：

```ts fetch.ts icon="/icons/typescript.svg"
const response = await fetch("https://bun.com/api", {
  method: "POST",
  body: JSON.stringify({ message: "Hello from Bun!" }),
  headers: { "Content-Type": "application/json" },
});

const body = await response.json();
```