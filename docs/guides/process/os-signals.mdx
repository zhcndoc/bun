---
title: 监听操作系统信号
sidebarTitle: 操作系统信号
mode: center
---

Bun 支持 Node.js 的全局对象 `process`，包括用于监听操作系统信号的 `process.on()` 方法。

```ts
process.on("SIGINT", () => {
  console.log("收到 SIGINT 信号");
});
```

---

如果你不知道要监听哪个信号，请监听 [`"beforeExit"`](https://nodejs.org/api/process.html#event-beforeexit) 和 [`"exit"`](https://nodejs.org/api/process.html#event-exit) 事件。

```ts
process.on("beforeExit", code => {
  console.log(`事件循环已空！`);
});

process.on("exit", code => {
  console.log(`进程以代码 ${code} 退出`);
});
```

---

有关更多实用工具，请参阅 [Utils](/runtime/utils)。
