---
title: 启动子进程
sidebarTitle: 启动子进程
mode: center
---

使用 [`Bun.spawn()`](/runtime/child-process) 启动子进程。

```ts
const proc = Bun.spawn(["echo", "hello"]);

// 等待完成
await proc.exited;
```

---

第二个参数是一个配置对象。

```ts
const proc = Bun.spawn(["echo", "Hello, world!"], {
  cwd: "/tmp",
  env: { FOO: "bar" },
  onExit(proc, exitCode, signalCode, error) {
    // 退出处理
  },
});
```

---

默认情况下，`proc.stdout` 是子进程 `stdout` 的 `ReadableStream`。

```ts
const proc = Bun.spawn(["echo", "hello"]);

const output = await proc.stdout.text();
output; // => "hello\n"
```

---

参阅[子进程](/runtime/child-process)。
