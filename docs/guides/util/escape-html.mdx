---
title: 转义 HTML 字符串
sidebarTitle: 转义 HTML
mode: center
---

`Bun.escapeHTML()` 会转义字符串中的 HTML 字符。它会进行以下替换。

- `"` 变为 `"&quot;"`
- `&` 变为 `"&amp;"`
- `'` 变为 `"&#x27;"`
- `<` 变为 `"&lt;"`
- `>` 变为 `"&gt;"`

此函数针对大输入进行了优化。非字符串值会先转换为字符串，然后再进行转义。

```ts
Bun.escapeHTML("<script>alert('Hello World!')</script>");
// &lt;script&gt;alert(&#x27;Hello World!&#x27;)&lt;/script&gt;
```

---

参见[工具](/runtime/utils)。
