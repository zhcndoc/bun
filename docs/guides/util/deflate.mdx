---
title: 使用 DEFLATE 进行数据压缩和解压
sidebarTitle: DEFLATE 压缩
mode: center
---

使用 `Bun.deflateSync()` 对 `Uint8Array` 进行 DEFLATE 压缩。

```ts
const data = Buffer.from("Hello, world!");
const compressed = Bun.deflateSync(data);
// => Uint8Array

const decompressed = Bun.inflateSync(compressed);
// => Uint8Array
```

---

请参阅 [Utils](/runtime/utils)。
