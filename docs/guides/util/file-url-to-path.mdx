---
title: 将文件 URL 转换为绝对路径
sidebarTitle: 文件 URL 转路径
mode: center
---

使用 `Bun.fileURLToPath()` 将 `file://` URL 转换为绝对路径。

```ts
Bun.fileURLToPath("file:///path/to/file.txt");
// => "/path/to/file.txt"
```

---

参见 [文档 > API > Utils](/runtime/utils) 获取更多实用工具。