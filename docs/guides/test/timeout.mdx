---
title: 使用 Bun 测试运行器设置单个测试超时
sidebarTitle: 测试超时
mode: center
---

使用 `--timeout` 标志以毫秒为单位设置每个测试的超时时间。超过此超时时间的测试将被标记为失败。

默认超时为 `5000`（5 秒）。

```sh terminal icon="terminal"
bun test --timeout 3000 # 3 秒
```

---

参见 [`bun test`](/test)。
