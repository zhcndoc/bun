---
title: 使用 Bun 测试运行器提前中止
sidebarTitle: 提前中止
mode: center
---

使用 `--bail` 标志在首次失败后中止测试运行，以便持续集成运行尽可能早地失败。

```sh terminal icon="terminal"
bun test --bail
```

---

要在达到特定失败次数后中止，请在标志后传入一个数字。

```sh terminal icon="terminal"
# 在失败达到 10 次后中止
bun test --bail=10
```

---

请参阅 [`bun test`](/test)。
