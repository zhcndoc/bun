---
title: 删除文件
sidebarTitle: 删除文件
mode: center
---

`Bun.file()` 函数接受一个路径并返回一个 `BunFile` 实例。使用 `.delete()` 方法来删除该文件。

```ts
const path = "/path/to/file.txt";
const file = Bun.file(path);

await file.delete();
```

---

请参阅 [`Bun.file()`](/runtime/file-io#reading-files-bun-file)。
