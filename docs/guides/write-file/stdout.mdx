---
title: 写入到标准输出
sidebarTitle: 写入到标准输出
mode: center
---

`console.log` 函数将内容写入 `stdout`，并在打印的数据末尾追加换行符。

```ts
console.log("Lorem ipsum");
```

---

Bun 还通过 `Bun.stdout` 属性将 `stdout` 暴露为 `BunFile`。将其作为目标传递给 [`Bun.write()`](/runtime/file-io#writing-files-bun-write)。

```ts
await Bun.write(Bun.stdout, "Lorem ipsum");
```

---

请参阅 [`Bun.write()`](/runtime/file-io#writing-files-bun-write)。
