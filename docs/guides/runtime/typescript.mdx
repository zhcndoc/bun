---
title: 为 Bun 安装 TypeScript 声明文件
sidebarTitle: TypeScript 类型
mode: center
---

要为项目添加 Bun 内置 API 的 TypeScript 类型定义，请安装 `@types/bun`。

```sh terminal icon="terminal"
bun add -d @types/bun # 开发依赖
```

---

以下是 Bun 项目推荐使用的完整 `compilerOptions` 配置。借助此 `tsconfig.json`，你可以使用顶层 await、带扩展名或不带扩展名的导入，以及 JSX。

```json tsconfig.json icon="file-json"
{
  "compilerOptions": {
    // 环境配置与最新特性
    "lib": ["ESNext"],
    "target": "ESNext",
    "module": "Preserve",
    "moduleDetection": "force",
    "jsx": "react-jsx",
    "allowJs": true,
    "types": ["bun"],

    // 打包模式
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,

    // 最佳实践
    "strict": true,
    "skipLibCheck": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,

    // 一些更严格的选项（默认关闭）
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noPropertyAccessFromIndexSignature": false
  }
}
```

---

如果你使用的是 TypeScript 6.0 或更高版本，还需要在 `compilerOptions` 中添加 `"types": ["bun"]`。详情请参阅 [TypeScript 6 和 7](/typescript-6)。

---

请参阅 [TypeScript](/runtime/typescript)。
