---
title: 删除目录
sidebarTitle: 删除目录
mode: center
---

要递归删除目录及其所有内容，请使用 `node:fs/promises` 中的 `rm`。这相当于在 JavaScript 中运行 `rm -rf`。

```ts delete-directory.ts icon="/icons/typescript.svg"
import { rm } from "node:fs/promises";

// 删除目录及其所有内容
await rm("path/to/directory", { recursive: true, force: true });
```

---

这些选项配置删除行为：

- `recursive: true` - 删除子目录及其内容
- `force: true` - 如果目录不存在，不抛出错误

省略 `force` 可在目录不存在时获取错误：

```ts delete-directory.ts icon="/icons/typescript.svg"
try {
  await rm("path/to/directory", { recursive: true });
} catch (error) {
  if (error.code === "ENOENT") {
    console.log("目录不存在");
  } else {
    throw error;
  }
}
```

---

请参阅[文件 I/O](/runtime/file-io) 了解更多文件系统操作。
