---
title: 添加一个 tarball 依赖
sidebarTitle: 添加一个 tarball 依赖
mode: center
---

Bun 的包管理器可以安装任何公开可用的 tarball URL 作为你的项目依赖。

```sh terminal icon="terminal"
bun add zod@https://registry.npmjs.org/zod/-/zod-3.21.4.tgz
```

---

此命令会下载、提取并将 tarball 安装到项目的 `node_modules` 目录中，同时将以下内容添加到你的 `package.json` 中：

```json package.json icon="file-json"
{
  "dependencies": {
    "zod": "https://registry.npmjs.org/zod/-/zod-3.21.4.tgz" // [!code ++]
  }
}
```

---

现在，你可以像往常一样导入 `zod`。

```ts
import { z } from "zod";
```

---

请参阅 [`bun install`](/pm/cli/install)。
