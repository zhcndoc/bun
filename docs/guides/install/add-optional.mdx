---
title: 添加可选依赖
sidebarTitle: 添加可选依赖
mode: center
---

要将 npm 包作为可选依赖添加，请使用 `--optional` 标志。

```sh terminal icon="terminal"
bun add zod --optional
```

---

这会将该包添加到 `package.json` 中的 `optionalDependencies`。

```json package.json icon="file-json"
{
  "optionalDependencies": {
    "zod": "^4.0.0" // [!code ++]
  }
}
```

---

请参阅 [`bun install`](/pm/cli/install)。
