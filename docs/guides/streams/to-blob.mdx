---
title: 将 ReadableStream 转换为 Blob
sidebarTitle: 流转换为 Blob
mode: center
---

`Bun.readableStreamToBlob` 将 [`ReadableStream`](https://developer.mozilla.org/en-US/docs/Web/API/ReadableStream) 的内容读取到 `Blob` 中。

```ts
const stream = new ReadableStream();
const blob = await Bun.readableStreamToBlob(stream);
```

---

请参阅 [Bun 的其他 `ReadableStream` 转换函数](/runtime/utils#bun-readablestreamto)。
